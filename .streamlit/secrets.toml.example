# Copy this file to ``secrets.toml`` and fill in your own key.
# ``secrets.toml`` is gitignored.
#
# On Streamlit Community Cloud, set this in the dashboard's
# Secrets panel rather than committing the file.

ANTHROPIC_API_KEY = "sk-ant-..."
